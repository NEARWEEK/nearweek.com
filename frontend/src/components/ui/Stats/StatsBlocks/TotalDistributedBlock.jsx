import React from "react";
import Block from "./Block/Block";

const TotalDistributedBlock = () => {
  return <Block title={"Distributed"} data={"-"} icon={true} />;
};

export default TotalDistributedBlock;
