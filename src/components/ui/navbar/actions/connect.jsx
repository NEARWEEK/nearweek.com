import * as React from "react";
import * as classes from "./connect.module.css";

const Connect = () => {
  return (
    <div className={classes.wrapper}>
      <a className={classes.btn} href="#">
        Connect
      </a>
    </div>
  );
};

export default Connect;
