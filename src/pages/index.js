import * as React from "react";
import HomePage from "./home";
import * as classes from "./index.module.css";

const IndexPage = () => {
  return <HomePage className={classes.body} />;
};

export default IndexPage;
